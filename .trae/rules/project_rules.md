1. 在wxml中，禁止使用text标签
2. 开始之前采访我
3. 生成less样式时，使用less的嵌套语法，减少CSS作用域污染
4. 涉及数据库操作，需要在有集合变动、字段变动、云函数变动等场景时，提醒我操作云数据库
5. 保持数据结构向前兼容（对于旧数据，必要时初始化新增字段的默认值）
6. 提高代码复用性和模块化，必要时抽取出单独的工具函数和组件，避免样式、代码和逻辑的多处重复
7. 在升级某个功能时，考虑到其他可能关联的模块，一并升级/兼容
