<view class="platform-selector">
  <label
    class="platform-option {{selectedPlatform === item._id ? 'selected' : ''}}"
    wx:for="{{platforms}}"
    wx:key="_id"
    bindtap="onSelect"
    data-id="{{item._id}}"
  >
    {{item.name}}
  </label>
</view>
