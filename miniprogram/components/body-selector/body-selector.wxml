<view class="body-image-container">
  <image
    class="body-image"
    src="/assets/human.jpeg"
    mode="aspectFit" />
  <view
    wx:for="{{parts}}"
    wx:key="_id"
    class="body-part {{item._id}} {{selectedParts[item._id] ? 'selected' : ''}}"
    bindtap="onPartTap"
    data-part="{{item._id}}"
  >
    <text class="part-name">{{item.name}}</text>
  </view>
</view>
