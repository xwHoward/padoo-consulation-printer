<view class="gender-selector">
  <label
    class="gender-option {{selectedGender === item._id ? 'selected' : ''}}"
    wx:for="{{genders}}"
    wx:key="_id"
    bindtap="onSelect"
    data-id="{{item._id}}"
  >
    {{item.name}}
  </label>
</view>
