<view class="checkbox-group">
  <label
    wx:for="{{strengths}}"
    wx:key="_id"
    class="checkbox-item {{selectedStrength === item._id ? 'selected' : ''}}"
    bindtap="onStrengthTap"
    data-strength="{{item._id}}"
  >
    <view class="strength-checkbox"></view>
    {{item.name}}
  </label>
</view>
